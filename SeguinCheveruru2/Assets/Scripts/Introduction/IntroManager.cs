using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntroManager : MonoBehaviour
{
    //private TextWriter writer;
    //public Animator fadeAnim;

    //public List<GameObject> firstSceneObjects;
    //public List<GameObject> secondSceneObjects;

    //private IEnumerator Start()
    //{
    //    secondSceneObjects.ForEach(t => t.SetActive(false));
    //    firstSceneObjects.ForEach(t => t.SetActive(true));

    //    writer = FindObjectOfType<TextWriter>();

    //    yield return new WaitForSeconds(2);
    //    yield return writer.WriteTexts(() => fadeAnim.SetTrigger("FadeOut"));
    //    yield return new WaitForSeconds(2.5f);
    //    fadeAnim.SetTrigger("FadeIn");
    //    secondSceneObjects.ForEach(t => t.SetActive(true));
    //    firstSceneObjects.ForEach(t => t.SetActive(false));
    //}

}
